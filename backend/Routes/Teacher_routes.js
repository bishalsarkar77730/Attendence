// import express from "express";
// import {
//   AuthenticatedUser,
//   verifyRole,
//   verifiedStatus,
// } from "../Utils/Auth.js";

// import { addTdata, updateTeacher } from "../Controllers/Teachers_controller.js";

// const router = express.Router();

// router.post("/:id", AuthenticatedUser, verifiedStatus, verifyRole("teacher"), addTdata);
// router.put("/:id", AuthenticatedUser, verifiedStatus, updateTeacher);

// export default router;
